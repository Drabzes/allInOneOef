import { DifficultyFilterPipe } from './difficulty-filter.pipe';

describe('DifficultyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
